{% extends "SgTuggenMainBundle::layout.html.twig" %}

{% if cmfMainContent.name != 'simple' %}
    {% set title = cmfMainContent.title %}
{% endif %}

{% block content %}

    <h1>{{ cmfMainContent.title }}</h1>

    <div id="page">
        {{ cmfMainContent.body|raw }}
        {# createphp cmfMainContent as="rdf" #}
        {# rdf|raw #}
        {# endcreatephp #}
    </div>

{% endblock %}
